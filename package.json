{
  "name": "typescript-node-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "start": "npm run build && node index.js",
    "build": "tsc --project tsconfig.json",
    "prebuild": "rm -rf index.js src/*.js",
    "reinit-git": "rm -rf .git && git init"
  },
  "author": "Braden Snell",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.0.2",
    "tslint": "^3.15.1"
  }
}
